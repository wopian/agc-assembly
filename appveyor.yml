environment:
  matrix:
    - nodejs_version: "7"
    - nodejs_version: "7.4"
platform:
  - x64
  - x86
cache:
  - "%LOCALAPPDATA%\\Yarn"
install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - yarn upgrade
  - yarn run vscode:install
  - yarn run vscode:prepublish
test_script:
  - npm t --silent
build: off
